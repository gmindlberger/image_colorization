Image Colorization with Autoencoder

This project implements a neural network for automatic colorization of grayscale images. It uses architectures like UNet and UNetRes to enhance image quality.

## Project Overview

The goal of this project is to convert grayscale images into color images. A dataset of landscape images, downloaded from Kaggle, is used for training. The model is trained using a custom dataset and a PyTorch DataLoader.

## Technologies Used

- **Programming Language**: Python
- **Frameworks**: PyTorch, torchvision, NumPy, Matplotlib
- **Additional Tools**: Kaggle API, PIL, tqdm

## Project Structure

- `data_prep.ipynb`: Script for preparing and renaming image files.
- `autoencoder.ipynb`: Main script for training and evaluating the model.
- `neuronal_nets`: Implementations of different model architectures.

## Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd <repository-name>


 TODO
1. Baseline: Regression
2. Hyperparameter Tuning: Grid-Search einbauen --> Check!
3. Dokumentation anhand CRISP DM - Lifecycle
	3.1 definiert selbst Problemstellung, Datensatz und Methoden
	3.2 Use-Case: ältere Mitmenschen

Crisp DM lifecycle:
1. Business Understanding
2. Data Understanding
3. Data Preparation
4. Modelling
5. Evaluation
6. Deployment

Jedes Modell 10x Epochen laufen lassen -> Screenshots abspeichern
Einen Score einbauen um es mit Testdaten zu vergleichen (Vergleichsscore für colorierte Bilder)
-> Diese dann speichern und das beste Modell dann auf 60 Epochen stellen.